syntax = "proto3";

import "github.com/micro/go-api/proto/api.proto";

service Form {
	// regular form
	rpc Submit(go.micro.api.Request) returns (go.micro.api.Response) {};
	// multipart form
	rpc Multipart(go.micro.api.Request) returns (go.micro.api.Response) {};
}
