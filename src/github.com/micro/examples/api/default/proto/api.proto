syntax = "proto3";

import "github.com/micro/go-api/proto/api.proto";

service Example {
	rpc Call(go.micro.api.Request) returns(go.micro.api.Response) {};
}

service Foo {
	rpc Bar(go.micro.api.Request) returns(go.micro.api.Response) {};
}
